<%= link_to api.api_name, api.address %>
     <% if current_user.admin?  %>
        <%= image_tag api.apicture.url if api.apicture? %>
       <%= link_to "delete", api, method: :delete,
             data: { confirm: "You are sure?" } %>
         <% end %>

